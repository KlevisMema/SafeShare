<style>
    .mud-dialog {
        width: 400px !important;
    }

        .mud-dialog .mud-dialog-actions {
            padding: 8px 24px 16px 24px !important;
        }

        .mud-dialog .mud-dialog-title {
            padding: 16px 24px 8px 24px !important;
        }
</style>

<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.Edit" Class="mr-3 mb-n1" />
            Change Password?
        </MudText>
    </TitleContent>
    <DialogContent>
        <EditForm @ref="@ChangePasswordForm" Model="UserChangePassword">
            <DataAnnotationsValidator />
            <MudTextField InputType="@PasswordInput" @bind-Value="@UserChangePassword.OldPassword" Label="Current Password"
                          Adornment="Adornment.End"
                          AdornmentIcon="@PasswordInputIcon"
                          AdornmentColor="Color.Primary"
                          OnAdornmentClick="DisplayTxtPassword"
                          AdornmentAriaLabel="Show Password"
                          Variant="Variant.Outlined"
                          Required=true />

            <MudTextField InputType="@PasswordInput" @bind-Value="@UserChangePassword.NewPassword" Label="New Password"
                          Adornment="Adornment.End"
                          AdornmentIcon="@PasswordInputIcon"
                          AdornmentColor="Color.Primary"
                          OnAdornmentClick="DisplayTxtPassword"
                          AdornmentAriaLabel="Show Password"
                          Variant="Variant.Outlined"
                          Required=true />

            <MudTextField InputType="@PasswordInput" @bind-Value="@UserChangePassword.ConfirmNewPassword" Label="Confirm new Password"
                          Adornment="Adornment.End"
                          AdornmentIcon="@PasswordInputIcon"
                          AdornmentColor="Color.Primary"
                          OnAdornmentClick="DisplayTxtPassword"
                          AdornmentAriaLabel="Show Password"
                          Variant="Variant.Outlined"
                          Required=true />
        </EditForm>
    </DialogContent>
    <DialogActions>
        <MudButton Disabled="_processing" Color="Color.Primary" ButtonType="ButtonType.Submit" Variant="Variant.Filled" OnClick="ValidateForm">
            @if (_processing)
            {
                <MudProgressCircular Class="ms-n1" Size="MudBlazor.Size.Small" Indeterminate="true" Color="Color.Primary" />
                <MudText Class="ms-2" Color="Color.Primary">Changing</MudText>
            }
            else
            {
                <MudText>Change Password</MudText>
            }
        </MudButton>
    </DialogActions>
</MudDialog>